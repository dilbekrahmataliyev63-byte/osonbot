<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Skaner</title>
    <script src="https://telegram.org"></script>
    <script src="https://unpkg.com"></script>
    <style>
        body { text-align: center; font-family: sans-serif; background: #f4f4f9; }
        video { width: 100%; max-width: 400px; border-radius: 10px; }
        button { padding: 10px 20px; font-size: 18px; background: #0088cc; color: white; border: none; border-radius: 5px; margin-top: 10px; }
    </style>
</head>
<body>
    <h3>Mahsulot kodini skaner qiling</h3>
    <video id="video"></video>
    <div id="result">Kutilmoqda...</div>
    <button onclick="startScan()">Kamerani yoqish</button>

    <script>
        const codeReader = new ZXing.BrowserMultiFormatReader();
        const tg = window.Telegram.WebApp;
        tg.expand();

        function startScan() {
            codeReader.decodeFromVideoDevice(null, 'video', (result, err) => {
                if (result) {
                    document.getElementById('result').textContent = "Kod: " + result.text;
                    // Natijani botga yuborish
                    tg.sendData(result.text);
                    tg.close();
                }
            });
        }
    </script>
</body>
</html>
